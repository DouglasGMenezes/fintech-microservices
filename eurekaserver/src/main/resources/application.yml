spring:
  application:
    name: eurekaserver

  security:
    user:
      name: eureka-user
      password: eureka-pass


server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
